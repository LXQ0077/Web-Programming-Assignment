url: https://leleartclub.eu-gb.mybluemix.net
The JWT token shown after successful login is for display only. The token will not be shown in the actual use. 

Folder "abc" includes my website. There is only one js file for server. To run the server or test, using command line to enter the folder "abc" and using "npm start" or "npm test". 

The port for local host is 8888, so the local url is http://localhost:8888/.

When using the local version, please do not log in to the system twice with the same device, becuase the session will recognise you as one user.
